spring.application.name=dnc-cachenode
server.port=${PORT:8081}
gateway.url=${GATEWAY_URL:http://localhost:8080}
node.advertised.host=${NODE_ADVERTISED_HOST:}

# Connection Details
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:cache1}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:Miki.234}
spring.jpa.hibernate.ddl-auto=update